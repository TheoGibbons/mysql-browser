# Standalone local defaults. Safe to run as-is: the site is published on
# loopback only and nothing here is a secret.
#
#   cp .env.example .env && bash scripts/up-local.sh
#   http://localhost:3084

SITE_BIND=127.0.0.1
SITE_PORT=3084
